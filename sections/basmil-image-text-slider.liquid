{% schema %}
{
  "name": "Image Text Slider-Basmil",
  "settings": [
    {
      "type": "text",
      "id": "custom_css_class",
      "label": "Custom CSS class",
      "info": "Add a class here to target this section from assets/custom.css"
    },
    {
      "type": "select",
      "id": "text_position",
      "label": "Text Position",
      "options": [
        {
          "value": "overlay",
          "label": "Overlay"
        },
        {
          "value": "side",
          "label": "Side-by-side"
        }
      ],
      "default": "overlay"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Autoplay",
      "default": true
    },
    {
      "type": "range",
      "id": "autoplay_speed",
      "label": "Autoplay Speed (ms)",
      "min": 1000,
      "max": 9000,
      "step": 500,
      "default": 3000
    },
    {
      "type": "number",
      "id": "slider_height",
      "label": "Slider Height (px)"
    },
    {
      "type": "image_picker",
      "id": "override_image",
      "label": "Override image",
      "info": "If set, this image is used for every slide instead of each slide's own image (e.g. the collection's image on collection pages)."
    },
    {
      "type": "header",
      "content": "Menu"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "info": "Shown as a horizontal nav under the heading, in Overlay text position only."
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text alignment",
      "options": [
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "left",
          "label": "Left"
        }
      ],
      "default": "center"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Slide Image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Image Text Slider-Basmil",
      "category": "Sliders",
      "blocks": [
        {
          "type": "slide"
        },
        {
          "type": "slide"
        },
        {
          "type": "slide"
        }
      ]
    }
  ]
}
{% endschema %}
{% assign position = section.settings.text_position %}
{% assign alignment = section.settings.text_alignment %}

<div
  class="image-text-slider {% if alignment == 'left' %} image-text-slider--align-left{% endif %} {% if position == 'side' %} page-width{% endif %}{% if section.settings.custom_css_class != blank %} {{ section.settings.custom_css_class }}{% endif %}"
  data-slick-slider
  data-slidesperview="1"
  data-slidestoscroll="1"
  data-autoplay="{{ section.settings.autoplay }}"
  data-autoplayspeed="{{ section.settings.autoplay_speed }}"
  data-arrows="true"
  data-dots="true"
  data-loop="true"
>
  {% for block in section.blocks %}
    {% if block.type == 'slide' %}
      {% assign slide_image = section.settings.override_image | default: block.settings.image %}
      <div class="slider-slide {{ position }}">
        <div class="slide-wrapper">
          {% if position == 'side' %}
            <div class="slide-content side-layout">
              <div class="slide-image">
                {% if slide_image %}
                  <img
                    src="{{ slide_image | image_url: width: slide_image.width, height: slide_image.height }}"
                    alt="{{ block.settings.heading | escape }}"
                    width="{{ slide_image.width }}"
                    height="{{ slide_image.height }}"
                  >
                {% endif %}
              </div>
              <div class="slide-text">
                <h1>{{ block.settings.heading }}</h1>
                <p>{{ block.settings.text }}</p>
              </div>
            </div>
          {% else %}
            <div class="slide-content overlay-layout">
              {% if slide_image %}
                <img
                  src="{{ slide_image | image_url: width: slide_image.width, height: slide_image.height }}"
                  alt="{{ block.settings.heading | escape }}"
                  width="{{ slide_image.width }}"
                  height="{{ slide_image.height }}"
                >
              {% endif %}
              <div class="overlay-text">
                <h1>{{ block.settings.heading }}</h1>
                {% if block.settings.text != blank %}
                  <p>{{ block.settings.text }}</p>
                {% endif %}
                {% if section.settings.menu != blank %}
                  <nav class="slide-menu">
                    <ul class="slide-menu__list">
                      {% for link in section.settings.menu.links %}
                        <li class="slide-menu__item{% if link.active %} slide-menu__item--active{% endif %}">
                          <a href="{{ link.url }}">{{ link.title }}</a>
                        </li>
                      {% endfor %}
                    </ul>
                  </nav>
                {% endif %}
              </div>
            </div>
          {% endif %}
        </div>
      </div>
    {% endif %}
  {% endfor %}
</div>

<style>
  #shopify-section-{{ section.id }} {
    padding-bottom:50px;

      .image-text-slider {
      position: relative;
      width: 100%;
      height: auto;
      min-height: 200px;

      }


      .slick-list {
      overflow:hidden!important;
      }
    .slick-arrow{
        top: 50%;
    }


      .slick-slide,.slick-track{
      margin: 0;
      transition: unset !important;
      }

      .image-text-slider{
      {% if position == 'side' %}
          margin-bottom:25px;
      {% endif %}
      }

      .image-text-slider .side-layout{
      min-height:100%;
      }

      .image-text-slider .slide-wrapper {
      position: relative;
      width: 100%;
      height: {{ section.settings.slider_height | minus: 14 }}px;
      min-height: 200px;
      overflow: hidden;
      }
      .image-text-slider .slide-content {
      height: 100%;
      }
      .image-text-slider .overlay-layout img,
      .image-text-slider .side-layout .slide-image img {
      height: 100%;
      max-height: 100%;
      }


      .image-text-slider .slick-slide img{
        --scale-factor: 1.1;
      transition: transform 0.9s ease-in-out;
      object-fit: cover;
      object-position: center;
      animation: 0.9s cubic-bezier(.25, .6, .5, .95) zoomOut;
      animation-fill-mode: forwards;
      min-width: 100%;
    }
    .image-text-slider .slick-slide.slick-active img {
      animation: 0.9s cubic-bezier(.25, .6, .5, .95) zoomOut;

      }

      .overlay-text {
      position: absolute;
      top: 50%;
      left: 50%;
      width: calc(100% - 60px);
      transform: translate(-50%, -50%);
      text-align: center;
      }

      .overlay-text:before{
        content: "";
        position: absolute;
        top: -100px;
        bottom: -100px;
        left: -200px;
        right: -200px;
        z-index: -1;
        border-radius: 50%;
        background: radial-gradient(#00000026, #0000 65%);
        }

        .overlay-text h1{
        font-size: 69.12px;
        font-weight: 700;
        {% if position == 'side' %}
          margin:0 0 .5em;
          color: #000;
        {% else %}
            color: #fff;
            margin:0;
        {% endif %}
        }

                .overlay-text p{
        font-size: 19.95px;
        margin:0 0 .5em;
        {% if position == 'side' %}
          color: #000;
        {% else %}
            color: #fff;
        {% endif %}
        }

      .image-text-slider--align-left .overlay-text {
      position: absolute;
      top: 50%;
      left: 0;
      right: 0;
      width: auto;
      /* Line the overlaid text up with the page-width container used by every
         other section, even though the section itself is full-bleed. */
      padding-left: max(60px, calc((100vw - var(--page-width)) / 2 + 60px));
      padding-right: max(60px, calc((100vw - var(--page-width)) / 2 + 60px));
      transform: translateY(-50%);
      text-align: left;
      }

      .image-text-slider--align-left .overlay-text:before {
      content: none;
      }

      .image-text-slider--align-left .overlay-text h1 {
      font-family: 'Overused Grotesk', var(--font-heading-family, sans-serif);
      font-weight: 400;
      font-size: 72px;
      line-height: 100%;
      letter-spacing: 0%;
      margin: 0 0 1rem;
      }

      .slide-menu__list {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 36px;
      list-style: none;
      margin: 0;
      padding: 0;
      }

      .slide-menu__item a {
      font-family: 'Overused Grotesk', var(--font-heading-family, sans-serif);
      font-weight: 400;
      font-size: 20px;
      line-height: 160%;
      letter-spacing: 0%;
      vertical-align: middle;
      color: #F7F6F3;
      text-decoration: none;
      opacity: 0.6;
      }

      .slide-menu__item--active a {
      opacity: 1;
      }

      @media (max-width: 768px) {
      .image-text-slider--align-left .overlay-text h1 {
        font-size: 40px;
      }
      .slide-menu__list {
        gap: 20px;
      }
      .slide-menu__item a {
        font-size: 16px;
      }
      }

      .image-text-slider .slick-dots {
      position: absolute;
      left: 0;
      right: 0;
      display: flex !important;
      justify-content: center;
      align-items: center;
      width: 100%;
      z-index: 10;
      padding: 0;
      margin: 0;
      pointer-events: auto;
      {% if position == 'side' %}
          bottom: -25px;
      {% else %}
          bottom: 25px;
      {% endif %}
      }
      .image-text-slider .slick-dots li {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: max-content;
      height: max-content;
      margin: 0 4px;
      padding: 0;
      border-radius: 50%;
      background: transparent;
      border: none;
      }
      .image-text-slider .slick-dots button {
      width: 10px;
      height: 10px;
      opacity: 0.5;
      border-radius: 50px;
      {% if position == 'side' %}
          border: 1px solid #000;
      {% else %}
          border: 1px solid #fff;
      {% endif %}
          background: transparent;
      transition: all 0.3s cubic-bezier(.4,0,.2,1);
      padding: 0;
      outline: none;
      box-sizing: border-box;
      display: block;
      margin: 0 auto;
      color:transparent;
      overflow: hidden;
      }
      .image-text-slider .slick-dots .slick-active button {
      width: 25px;
      height: 10px;
      border-radius: 50px;
      opacity: 1;
      }
      .image-text-slider .side-layout {
      display: flex;
      flex-wrap: wrap;
      }
      .image-text-slider .side-layout .slide-image {
        flex: 1 1 50%;
      }
      .image-text-slider .side-layout .slide-image img {
        width: 100%;
        object-fit: cover;
        object-position: center;
        display: block;
      }

      .image-text-slider .side-layout .slide-text {
        flex: 1 1 50%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        padding: 2rem;
      }

      @media (min-width: 1025px) {
              .image-text-slider .slide-wrapper {
                  height: {{ section.settings.slider_height | default: 364 }}px;
              }
        }

      @media (max-width: 768px) {
        .overlay-text {
            position: absolute;
            top: 50%;
            left: 50%;
            width: calc(100% - 60px);
            transform: translate(-50%, -50%);
        }
    }
  }
</style>
